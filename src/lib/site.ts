export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://growthsocialhq.com";

/**
 * Where creators apply. Referenced from the landing page's creator CTA and
 * from the Academy login's "Request access" — kept here so the two can't
 * drift apart.
 */
export const APPLY_URL = "https://apply.growthsocialhq.com";
